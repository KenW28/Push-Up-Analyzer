package main

// db.go is reserved for persistence code (database access).
// Right now it's intentionally empty so the server can compile.
// Later we will put:
// - User storage (username + password hash)
// - Leaderboard storage / queries
